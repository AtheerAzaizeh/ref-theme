{{ 'section-header-minimal.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .minimal-header {
    padding: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px 0 {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .minimal-header {
      padding: {{ section.settings.padding_top }}px 0 {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<header class="minimal-header color-{{ section.settings.color_scheme }} gradient">
  <div class="minimal-header__container page-width">
    {%- comment -%} Left Menu {%- endcomment -%}
    <nav class="minimal-header__nav">
      <ul class="minimal-header__menu">
        {%- if section.settings.show_about_link -%}
          <li class="minimal-header__menu-item">
            <a href="{{ section.settings.about_link | default: '/pages/about-us' }}" class="minimal-header__menu-link">
              {{ section.settings.about_text | default: 'About' }}
            </a>
          </li>
        {%- endif -%}
        {%- if section.settings.show_contact_link -%}
          <li class="minimal-header__menu-item">
            <a href="{{ section.settings.contact_link | default: '/pages/contact' }}" class="minimal-header__menu-link">
              {{ section.settings.contact_text | default: 'Contact' }}
            </a>
          </li>
        {%- endif -%}
      </ul>
    </nav>

    {%- comment -%} Center Logo {%- endcomment -%}
    <div class="minimal-header__logo">
      {%- if request.page_type == 'index' -%}
        <h1 class="minimal-header__heading">
      {%- endif -%}
      <a href="{{ routes.root_url }}" class="minimal-header__logo-link">
        {%- if section.settings.logo != blank -%}
          <img
            src="{{ section.settings.logo | image_url: width: 400 }}"
            alt="{{ section.settings.logo.alt | default: shop.name | escape }}"
            class="minimal-header__logo-image"
            width="{{ section.settings.logo_width }}"
            height="{{ section.settings.logo_width | divided_by: section.settings.logo.aspect_ratio | round }}"
            loading="eager"
          >
        {%- elsif settings.logo != blank -%}
          <img
            src="{{ settings.logo | image_url: width: 400 }}"
            alt="{{ settings.logo.alt | default: shop.name | escape }}"
            class="minimal-header__logo-image"
            width="{{ settings.logo_width }}"
            height="{{ settings.logo_width | divided_by: settings.logo.aspect_ratio | round }}"
            loading="eager"
          >
        {%- else -%}
          <span class="minimal-header__shop-name">{{ shop.name }}</span>
        {%- endif -%}
      </a>
      {%- if request.page_type == 'index' -%}
        </h1>
      {%- endif -%}
    </div>

    {%- comment -%} Right Cart Icon {%- endcomment -%}
    <div class="minimal-header__actions">
      {%- if section.settings.show_cart -%}
        <a href="{{ routes.cart_url }}" class="minimal-header__cart-link" aria-label="{{ 'sections.header.cart' | t }}">
          <svg
            class="minimal-header__cart-icon"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
          >
            <path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"></path>
            <line x1="3" y1="6" x2="21" y2="6"></line>
            <path d="M16 10a4 4 0 01-8 0"></path>
          </svg>
          {%- if cart != empty -%}
            <span class="minimal-header__cart-count">{{ cart.item_count }}</span>
          {%- endif -%}
        </a>
      {%- endif -%}
    </div>
  </div>
</header>

{% schema %}
{
  "name": "Header",
  "class": "section-header",
  "settings": [
    {
      "type": "header",
      "content": "Logo"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo image"
    },
    {
      "type": "range",
      "id": "logo_width",
      "min": 50,
      "max": 300,
      "step": 10,
      "default": 120,
      "unit": "px",
      "label": "Logo width"
    },
    {
      "type": "header",
      "content": "Menu Links"
    },
    {
      "type": "checkbox",
      "id": "show_about_link",
      "label": "Show About link",
      "default": true
    },
    {
      "type": "text",
      "id": "about_text",
      "label": "About link text",
      "default": "About"
    },
    {
      "type": "url",
      "id": "about_link",
      "label": "About link URL"
    },
    {
      "type": "checkbox",
      "id": "show_contact_link",
      "label": "Show Contact link",
      "default": true
    },
    {
      "type": "text",
      "id": "contact_text",
      "label": "Contact link text",
      "default": "Contact"
    },
    {
      "type": "url",
      "id": "contact_link",
      "label": "Contact link URL"
    },
    {
      "type": "header",
      "content": "Cart"
    },
    {
      "type": "checkbox",
      "id": "show_cart",
      "label": "Show cart icon",
      "default": true
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 60,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 20
    }
  ]
}
{% endschema %}
